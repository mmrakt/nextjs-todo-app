import { extendTheme } from '@chakra-ui/react'
const chakuraUiTheme = extendTheme({
  colors: {
    dark: {
      black: '#121212',
      gray: '#1E1E1E',
      lightGray: '#808080',
    },
  },
})

export default chakuraUiTheme
